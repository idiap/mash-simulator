if (APPLE)
    set(CONFIG_PLUGIN_FOLDER "./")
    set(CONFIG_MEDIA_PATH_PREFIX "../")
else()
    set(CONFIG_PLUGIN_FOLDER "./")
    set(CONFIG_MEDIA_PATH_PREFIX "")
endif()
    
file(RELATIVE_PATH CONFIG_ATHENA_MEDIA_PATH "${CMAKE_RUNTIME_OUTPUT_DIRECTORY}" "${ATHENA_FRAMEWORK_SOURCE_DIR}/README")
string(REPLACE "README" "media" CONFIG_ATHENA_MEDIA_PATH ${CONFIG_ATHENA_MEDIA_PATH})
set(CONFIG_ATHENA_MEDIA_PATH "${CONFIG_MEDIA_PATH_PREFIX}${CONFIG_ATHENA_MEDIA_PATH}")

file(RELATIVE_PATH CONFIG_MASH_MEDIA_PATH "${CMAKE_RUNTIME_OUTPUT_DIRECTORY}" "${MASH_SIMULATOR_SOURCE_DIR}/CMakeLists.txt")
string(REPLACE "CMakeLists.txt" "media" CONFIG_MASH_MEDIA_PATH ${CONFIG_MASH_MEDIA_PATH})
set(CONFIG_MASH_MEDIA_PATH "${CONFIG_MEDIA_PATH_PREFIX}${CONFIG_MASH_MEDIA_PATH}")

if (UNIX)
    configure_file(${MASH_SIMULATOR_SOURCE_DIR}/config/athena_linux.cfg.in
                   ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/athena.cfg)
else()
    configure_file(${MASH_SIMULATOR_SOURCE_DIR}/config/athena.cfg.in
                   ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/athena.cfg)
endif()
